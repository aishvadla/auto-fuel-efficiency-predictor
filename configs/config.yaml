model:
  # input_features NOT here — computed dynamically
  hidden_units: [8, 4]
  output_features: 1  # design choice - always 1 for regression. For classification, compute from np.unique(y) data

training:
  epochs: 100
  log_epochs: 10
  batch_size: 32
  eta: 0.005

data:
  test_size: 0.15
  val_size: 0.15
  random_state: 24